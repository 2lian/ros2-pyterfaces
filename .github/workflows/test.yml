name: Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:

    strategy:
      fail-fast: false
      matrix:
        ros-version: ["humble", "jazzy", "kilted"]
        os: [ubuntu-latest, windows-latest]

    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v5
    - uses: prefix-dev/setup-pixi@v0.9.4
      with:
        cache: true
        environments: ${{ matrix.ros-version }}
    - name: Test with pixi+pytest
      run: |
        pixi run -e ${{ matrix.ros-version }} pytest ./tests -vv

